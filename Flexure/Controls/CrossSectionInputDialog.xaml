<Window x:Class="ACI318_19Library.CrossSectionInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ACI318_19Library"
        Title="Cross Section Input"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        MinWidth="400" MinHeight="300">

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <!-- content scrolls if needed -->
            <RowDefinition Height="Auto"/>
            <!-- buttons -->
        </Grid.RowDefinitions>

        <!-- Scrollable content -->
        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel x:Name="spControl">
                <!--<local:CrossSectionInputControl x:Name="crossSectionControl" Width="350"/>-->
            </StackPanel>
        </ScrollViewer>

        <!-- Buttons -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="1" Margin="4">
            <Button Content="OK" Width="75" Margin="2" IsDefault="True" Click="Ok_Click"/>
            <Button Content="Cancel" Width="75" Margin="2" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
